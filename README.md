# Torched Glory

Starter landing page for Torched Glory, a flag company that will sell ready-made flags and offer custom logo flag orders.

## Local preview

Open `index.html` in a browser, or run a quick static server:

```bash
python3 -m http.server 4173
```

Then visit <http://localhost:4173>.
